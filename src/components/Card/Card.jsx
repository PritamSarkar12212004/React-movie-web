import React, { useContext } from "react";
import Cards from "./Cards";
function Card() {
  return (
    <div className="w-full h-screen flex  ">
      <Cards />
    </div>
  );
}

export default Card;
